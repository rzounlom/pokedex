import { Container } from "react-bootstrap";
import { FC } from "react";

const EditPokemon: FC = () => {
  return <Container className="page">EditPokemon</Container>;
};

export default EditPokemon;
